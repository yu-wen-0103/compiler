README:
	You only need to enter "make", it will compile the .g file and myChecker_test.java. And finally it will print the output that professor requires. 
	In the makefile,because I put ANTRL tools on the last file, I write "../antlr~" to get the ANTLR tools.